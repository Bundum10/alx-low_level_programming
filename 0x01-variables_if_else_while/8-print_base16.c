#include<stdio.h>

/**
 * main -Entry point
 *
 * Return: 0 (success)
 *
 */
int main(void)
{
	char a;
a =48;
	while(a < 58)
	{
		putchar(a);
		++a;
	}
a=97;
	while(a < 103)
	{
		putchar(a);
		++a;
	}
		r('\n')
